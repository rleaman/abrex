# T057 Freeze the development guidelines and challenge views

Owner: **Luna — engineering and preparation**  
Status: Planned; this file does not authorize automatic execution.  
Dependencies: T056 returned user supplement; unresolved decisions remain explicit.

## Execution contract

Read [the milestone plan](../post-t052-next-steps.md), [Luna's guide](LUNA_EXECUTION_GUIDE.md), AGENTS.md and the specific dependency outputs. The plan's common acceptance and scientific safeguards are part of this task. Existing interfaces are starting points, not a requirement to duplicate them.

## Implementation steps

1. Validate and summarize the user's actual revisions, disagreements with initial suggestions, incomplete passage searches and unresolved questions. Preserve the original T052 bundle.
2. Create a new dated scientific-model iteration describing what changed and why, with positive, negative and uncertain examples. Do not rewrite the September 10 iteration.
3. Write a versioned guideline supplement operationalizing only user-settled distinctions. Record the definitions of strict abbreviation inclusion, evidence-fragment selection, reconstruction, source errors, lists, ratios and uncertainty. Proposals still unresolved remain outside scored claims.
4. Materialize a named strict exact-pair development view using existing scientific contracts, plus a diagnostic view preserving broader and unrepresentable evidence. Produce an eligibility ledger for every relation and case. This task does not invent a relaxed metric.
5. Preserve explicit out-of-scope candidates: later predictions outside the strict target must be classified according to the frozen task policy, not silently discarded because they fail a positive filter. Partially reviewed passages cannot produce valid ordinary precision/recall denominators.
6. Freeze a new versioned evidence bundle with source hashes, user provenance, guideline identity, eligible units and limitations. T052 remains development material; no untouched-test claim.

## Acceptance, deliverables and stopping point

Every input decision maps to a retained output or explicit eligibility reason. Tests cover policy selection, incomplete cases, unresolved annotations and exact-span preservation. Deliver new iteration, guidelines, manifest and challenge views; update scientific-model/evidence indexes. Do not certify independent gold or start method runs.

