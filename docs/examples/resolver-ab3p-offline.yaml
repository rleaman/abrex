# Windows/offline replay. No Ab3P executable is needed.
resolver:
  type: ab3p
  params:
    backend: cache_only
    cache:
      path: artifacts/ab3p-cache
      read: true
      write: false
    # Must match the immutable identity used when the cache was populated.
    installation_label: nlm-linux-ab3p
