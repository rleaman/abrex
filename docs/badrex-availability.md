# Dataset availability note

The project originally planned to use the `medstract_badrex` and
`schwartz_hearst_badrex` datasets. Their source URLs now return 404, so these
datasets are not configured or registered in the project.
